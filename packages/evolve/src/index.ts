/**
 * @friday/evolve — self-improvement pipeline.
 *
 * Phase 1 scaffold: type and signal exports. The full scanner + enrichment +
 * cluster pipeline lands in Phase 6 (and lifts heavily from the existing
 * package, replacing the Beads dispatch with direct mail + ticket writes).
 */

export * from "./types.js";
export * from "./store.js";
