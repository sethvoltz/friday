# Documents the shape of an app's .env. NOT loaded by the installer or
# the daemon — copy to `.env` (gitignored by default) at install time
# only if your app actually needs secrets.
EXAMPLE_API_KEY=replace-me
