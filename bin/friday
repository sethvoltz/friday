#!/usr/bin/env bash
# Dev shim — runs @friday/cli via tsx from the monorepo
# In production, install @friday/cli globally instead.

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
ROOT_DIR="$(dirname "$SCRIPT_DIR")"

exec npx tsx "$ROOT_DIR/packages/cli/src/index.ts" "$@"
