#!/usr/bin/env bash
# Dev shim — runs @friday/evolve CLI via tsx from the monorepo
# In production, install @friday/evolve globally instead.

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
ROOT_DIR="$(dirname "$SCRIPT_DIR")"

exec npx tsx "$ROOT_DIR/packages/evolve/src/cli.ts" "$@"
